name: ELO Totals Interval Scan

on:
  workflow_dispatch:
  push:
    paths:
      - "data/tennis_totals_results.json"
      - "ratings/tennis_elo.json"
      - "ratings/elo_lookup.py"
      - "ratings/elo_totals_interval_scan.py"
      - ".github/workflows/elo_totals_interval_scan.yml"

permissions:
  contents: write

concurrency:
  group: elo-totals-interval-scan
  cancel-in-progress: false

jobs:
  elo-totals-interval-scan:
    runs-on: ubuntu-latest
    timeout-minutes: 20

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          persist-credentials: true

      - name: Pull latest changes
        run: |
          git config user.name "github-actions"
          git config user.email "github-actions@github.com"
          git pull --rebase --autostash origin main

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Check source files
        run: |
          test -f data/tennis_totals_results.json
          test -f ratings/tennis_elo.json
          test -f ratings/elo_lookup.py
          test -f ratings/elo_totals_interval_scan.py
          echo "All source files found."

      - name: Run ELO totals interval scan
        run: |
          python ratings/elo_totals_interval_scan.py

      - name: Commit interval scan
        run: |
          git config user.name "github-actions"
          git config user.email "github-actions@github.com"

          git add ratings/elo_totals_interval_scan.json
          git add ratings/elo_totals_interval_scan_table.md

          if git diff --cached --quiet; then
            echo "No interval scan changes to commit."
            exit 0
          fi

          git commit -m "Update ELO totals interval scan"
          git pull --rebase --autostash origin main
          git push origin main

      - name: Upload interval scan artifact
        uses: actions/upload-artifact@v4
        with:
          name: elo-totals-interval-scan
          path: |
            ratings/elo_totals_interval_scan.json
            ratings/elo_totals_interval_scan_table.md
          if-no-files-found: error
